server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Single-page app: every route (/dashboard, /link/...) falls back to index.html;
    # React Router takes it from there.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
